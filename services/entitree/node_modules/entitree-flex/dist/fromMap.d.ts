import { Settings } from "./Settings";
import { TreeNode } from "./TreeNode";
export declare function fromMap<T>(rootId: string, originalMap: Record<string, T>, customSettings?: Partial<Settings>): {
    map: Record<string, TreeNode<T>>;
    nodes: TreeNode<T>[];
    rels: {
        source: TreeNode<T>;
        target: TreeNode<T>;
    }[];
    maxRight: number;
    maxLeft: number;
    maxBottom: number;
    maxTop: number;
};
