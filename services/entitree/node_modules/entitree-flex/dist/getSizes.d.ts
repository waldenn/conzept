import { Settings } from "./Settings";
import { TreeMap } from "./TreeMap";
export declare const getSizes: <T>(rootId: string, map: TreeMap<T>, customSettings?: Partial<Settings>) => {
    maxRight: number;
    maxLeft: number;
    maxBottom: number;
    maxTop: number;
};
