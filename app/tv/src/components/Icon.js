import React from 'react';

const Icon = ({ children }) => {
  return (
    <div className='d-flex justify-content-center align-items-center'>
      {children}
    </div>
  );
};

export default Icon;
